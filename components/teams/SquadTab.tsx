'use client'
// Placeholder — volledig ingevuld in een latere taak.
export default function SquadTab({ teamName }: { teamName: string }) {
  return <div style={{ fontSize: 10, color: 'var(--color-muted)', padding: 12 }}>{teamName}</div>
}
